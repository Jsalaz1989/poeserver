setup:
    config:
        env_file: .env.prod
    #     S3_BUCKET: my-example-bucket0
    # addons:
    #     - plan: heroku-postgresql
    #       as: DATABASE
build:
    docker:
        web: image/app/Dockerfile.prod
        # worker: worker/Dockerfile.prod
    config:
        DJANGO_ENV: development
# release:
#     command:
#         - ./deployment-tasks.sh
#     image: worker
run:
    web: gunicorn server.wsgi:application --bind 0.0.0.0:$PORT --log-level debug --access-logfile - --error-logfile -
#     worker: python myworker.py
#     asset-syncer:
#         command:
#             - python asset-syncer.py
#         image: worker
